# Example production environment file for agency-service
# Copy this to `.env.prod` and fill in real values on your local machine/server.

# Runtime
NODE_ENV=production

# Database (used by Nest/TypeORM)
DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=app_db

# Postgres container bootstrap (used by db service in docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app_db

# Auth / JWT
AUTH_JWT_SECRET=change_me

# Firebase / FCM (encrypted service account etc.)
FIREBASE_SA_ENC=change_me
FIREBASE_SA_KEY=change_me

# SMS provider
SMS_TOKEN=change_me
SMS_URL=https://sms.aakashsms.com/sms/v3/send/

# Forex API
FASTFOREX_API_KEY=change_me
FASTFOREX_API_URL=https://api.fastforex.io

# Add any additional feature flags / config you currently use here
