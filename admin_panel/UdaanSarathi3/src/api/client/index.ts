/**
 * HTTP Client exports
 * 
 * This module exports the HTTP client and related utilities for making
 * type-safe API requests.
 */

export { HttpClient, httpClient } from './httpClient';
export type { HttpClientConfig, RequestConfig } from './httpClient';
